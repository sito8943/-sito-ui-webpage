function Permissions() {
  return (
    <section className="sgSection">
      <h2 className="sgH2">Permissions</h2>
      <ul className="sgList">
        <li>
          <code className="sgCode">storage</code> — used to save <code className="sgCode">username</code> and <code className="sgCode">profile_name</code> locally.
        </li>
      </ul>
    </section>
  );
}

export default Permissions;

